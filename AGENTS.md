# はまむび！開発ルール

## ページレイアウト

- 新しい画面は `src/PageLayout.tsx` の `PageShell` と `PageHeader` を再利用する。画面ごとに本文幅、左右余白、ページ見出しを独自実装しない。
- ページ見出しは「短い補足（eyebrow）→ h1 → 必要な場合だけリード文・メタ情報」の順にする。1画面の `h1` は1つにする。
- 情報のまとまりは、カードを増やす前に余白、見出し、区切り線で表現する。既存のダークテーマと色トークンを使う。
- モバイルを基準に単一カラムで設計する。広い画面向けの複数カラムはメディアクエリ内で追加し、狭幅の明示的なフォールバックを残す。

## モバイル品質

- 320px と 390px の両方で確認し、ページ全体の横スクロールを発生させない。Flex/Grid の可変領域には `min-width: 0` を設定する。
- ページ左右余白は共有シェルに任せ、`100vw` を本文幅に使わない。横スクロールは日付やフィルタなど、意図した局所UIだけに限定する。
- 主操作のタップ領域は原則44px以上にする。フォーカス表示、見出し構造、ラベル、キーボード操作を維持する。
- スマホのサイドバーはデフォルトで閉じた状態を保つ。

## ルーティングと検証

- 各画面・日付・作品のハッシュURLを維持し、直接アクセス、再読み込み、戻る・進むを確認する。既存ハッシュを変更する場合は旧URLとの互換性を残す。
- UI変更後は `npm run ci:pr` を実行する。
- ブラウザで少なくとも 320x700 と 390x844 を確認し、横方向オーバーフロー、見出しの折り返し、主要操作を検証する。
- マージ後は Cloudflare Pages の本番URLで対象ハッシュを直接開き、表示とナビゲーションを確認する。
